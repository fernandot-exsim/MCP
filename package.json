{
  "name": "mongo-mcp-railway-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "MDB_MCP_HTTP_PORT=${PORT:-3000} npx -y mongodb-mcp-server@latest --transport http --httpHost=0.0.0.0 --readOnly --indexCheck"
  }
}
